# Session 2026-03-31-003

## Goals
- Answer: are automated tests appropriate for this project?
- Produce manual test checklist
- Diagnose and fix bugs: sticky avatar under battery panel, right-side posts shifted left

## Steps

| Step | Status | Notes |
|------|--------|-------|
| Fix right bubble alignment (align-items: flex-end on .bubbles in .right) | done | regression from sticky avatar commit |
| Fix avatar under status bar (bottom: 36px desktop, calc(--panel-width + 36px) mobile) | done | status-bar is 28px fixed at bottom |
| Add exports + entry-point guard to process.js | done | export pure functions, guard main() |
| Create scripts/test.js (21 tests, node:test) | done | 21/21 pass |
| Strengthen content protection in CLAUDE.md | done | hard rule, no exceptions section |
| Update commit format directive in CLAUDE.md | done | full git add/commit/push block |
| Session closed | done | |

## Notes

Баг с правыми пузырями — регрессия из коммита sticky avatar: `.message-row` перестал быть flex-контейнером, `.bubbles` внутри `.message-group.right` не имел `align-items: flex-end`, поэтому пузыри лепились к левому краю bubbles-колонки.

Аватар под статус-бар: `bottom: 8px` не учитывал высоту `.status-bar` (28px fixed). Итого 36px на десктопе, плюс `var(--panel-width)` на мобайле.

Один тест изначально тестировал некорректное предположение о формате заголовка — исправлено.
