# Session 2026-03-31-002

## Goals
- Fix *** dividers: some applied correctly, some remained as raw separators
- Increase contrast on divider rendering
- Fix direct speech color (gets lost)
- Fix mixed blocks: speech embedded in text block causes whole-block formatting or no formatting

## Log

| Step | Status | Notes |
|------|--------|-------|
| Add `--dialogue: #d4c49a` to common.css | done | Warm parchment, readable on dark bg |
| Update `.dialogue` color in reader.css | done | From `--text-dim` to `--dialogue` |
| Improve divider contrast in reader.css | done | Both bubble-divider and scene-divider: `--text-dim` + `--border` (no opacity 0.5) |
| Fix LOD cross-batch divider merge | done | Pending divider mechanism: defer trailing dividers, extend last bubble on next batch |
| Inline dialogue detection (`_markDialogue`) | done | Regex wraps `[-–—] TEXT` segments in `<span class="dialogue">` per-segment |
| Extract `_renderContentLines` helper | done | Shared between `_makeMessage` and `_extendLastBubble` |
| Add `_extendLastBubble` for cross-batch merge | done | insertAdjacentHTML + re-bind char links |
| Update `_loadNextBatch` for pending handling | done | prevPending check before rendering new batch |
| Update `_teardownLOD` to flush pending | done | Edge case: trailing divider at very end of all content |

## Notes
